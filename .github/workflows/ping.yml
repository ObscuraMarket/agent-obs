# A one-step workflow to prove this repo can start a job at all.
name: Ping
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions can start jobs on this repo"
