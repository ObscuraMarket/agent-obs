export const environment = {
  production: true,
  // Point this at wherever agent-obs's read-only dashboard server is hosted.
  // That host must list this site's origin in OBS_DASHBOARD_ORIGINS.
  obsApiUrl: 'https://obs-api.obscura.markets'
};
